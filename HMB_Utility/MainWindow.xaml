<Window x:Class="HMB_Utility.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HMB_Utility"
        mc:Ignorable="d"
        Title="HBM" Height="600" Width="700">






    <Grid x:Name="baseGrid" ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" MinHeight="150" MaxHeight="200"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="163" MaxWidth="350"/>
            <ColumnDefinition Width="187"/>
            <ColumnDefinition />

        </Grid.ColumnDefinitions>

        <!--<Button x:Name="SearchDeviceButton" Content="Search Devices" HorizontalAlignment="Left" Margin="39,10,0,0" VerticalAlignment="Top" Width="102"
                Click="SearchDeviceButton_Click" Grid.Column="2" Grid.Row="0" Height="20" />-->
        <Button x:Name="SearchDeviceButton" Command="{Binding SearchCommand}" Content="Search Devices" HorizontalAlignment="Left" Margin="39,10,0,0" VerticalAlignment="Top" Width="102" Grid.Column="2" Grid.Row="0" Height="20" />
        <Button x:Name="Connect_btn" Command="{Binding ConnectCommand}" Content="Connect" Grid.Column="2" HorizontalAlignment="Left" Margin="153,10,0,0" VerticalAlignment="Top" Width="75" Height="20"/>

        <!--<TreeView x:Name="treeView1" Width="150" Height="500" ItemsSource="{Binding AllDevices}" >
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Signals}">
                    <TextBlock Text="{Binding Name}"/>
                    <HierarchicalDataTemplate.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>-->


        <ListBox Name="DeviceListBox" ItemsSource="{Binding AllDevices}" SelectionMode="Extended"
                 SelectedItem="{Binding SelectedDevice}" SelectionChanged="DeviceListBox_SelectionChanged" Margin="0,0,0,266" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock FontSize="18" Text="{Binding Path=Name}" />
                        <TextBlock Text="{Binding Path=IpAddress}" />
                        <TextBlock Text="{Binding Path=Model}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Column="2" DataContext="{Binding SelectedDevice}" Margin="10,42,145,163">
            <TextBlock Text="Выбранный элемент"/>
            <TextBlock Text="Имя" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="IP адрес" />
            <TextBox Text="{Binding IpAddress, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Модель" />
            <TextBox Text="{Binding Model, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>




        <GridSplitter Grid.Row="0" ShowsPreview="True" Width="3" VerticalAlignment="Stretch" Grid.Column="1"/>
        <Button x:Name="Btn1" Content="Get single measurment" Grid.Column="2" HorizontalAlignment="Left" Margin="153,301,0,0" VerticalAlignment="Top" Width="149" Height="20"/>
        <TextBox Name ="LogBox" HorizontalAlignment="Left" Height="130" Margin="10,10,0,0" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" 
                 TextWrapping="Wrap" Text="Logs" VerticalAlignment="Top" Width="672"  />
        <Button x:Name="Btn3" Content="Create DB" Grid.Column="2" HorizontalAlignment="Left" Margin="233,35,0,0" VerticalAlignment="Top" Width="75" Height="20"/>
        <Button x:Name="Btn4" Content="Start DAQ" Grid.Column="2" HorizontalAlignment="Left" Margin="233,69,0,0" VerticalAlignment="Top" Width="75" Height="20"/>
        <Button x:Name="Btn5" Content="Stop DAQ" Grid.Column="2" HorizontalAlignment="Left" Margin="233,104,0,0" VerticalAlignment="Top" Width="75" Height="20"/>

        <!--<ListBox x:Name="SignalListBox" DataContext="{Binding SelectedDevice}" ItemsSource="{Binding Path = Signals}" SelectionMode="Extended"
             Grid.Column="1" Margin="10,0,14,266" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock FontSize="10" Text="{Binding Path = Name}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->

        <ListBox x:Name="SignalListBox" DataContext="{Binding SelectedDevice}" ItemsSource="{Binding Path = Signals}" SelectionMode="Extended"
             Grid.Column="1" Margin="10,0,14,266" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock FontSize="10" Text="{Binding Path = Name}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>


        <Button Content="Button" Grid.Column="2" HorizontalAlignment="Left" Margin="118,373,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click"/>

        <!--<TreeView x:Name="treeView1" Margin="0,158,0,0" ItemsSource="{Binding AllDevices}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Signals}">
                    <TextBlock Text="{Binding Name}" />
                    <HierarchicalDataTemplate.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>-->



        <!--<GridSplitter Grid.Row="1" Grid.ColumnSpan="2" Height="3"
        HorizontalAlignment="Stretch" VerticalAlignment="Top" ShowsPreview="True"/>-->
    </Grid>
</Window>
